<UserControl x:Class="HowLongToBeat.Controls.HltbPieChartTooltip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:wpf="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d"
             d:DesignHeight="80"
             d:DesignWidth="160">

    <UserControl.Template>
        <ControlTemplate>
            <Border Background="{DynamicResource CommonToolTipBackgroundBrush}"
                    CornerRadius="{DynamicResource CommonToolTipCornerRadius}"
                    BorderThickness="{DynamicResource CommonToolTipBorderThickness}"
                    Padding="{DynamicResource CommonToolTipPadding}"
                    BorderBrush="{DynamicResource CommonToolTipBorderBrush}">
                <ItemsControl ItemsSource="{Binding Data.Points}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type wpf:DataPointViewModel}">
                            <StackPanel Orientation="Horizontal"
                                        Margin="2">
                                <TextBlock Text="{Binding Series.Title}"
                                           Style="{DynamicResource BaseTextBlockStyle}"
                                           FontSize="14"
                                           VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding ChartPoint.Y, StringFormat={}{0:0}}"
                                           Style="{DynamicResource BaseTextBlockStyle}"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Margin="8,0,0,0"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Border>
        </ControlTemplate>
    </UserControl.Template>
</UserControl>
