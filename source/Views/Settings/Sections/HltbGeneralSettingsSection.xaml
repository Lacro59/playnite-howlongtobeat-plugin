<UserControl x:Class="HowLongToBeat.Views.HltbGeneralSettingsSection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:AttachedProperties="clr-namespace:CommonPluginsShared.Controls"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:controls="clr-namespace:CommonPluginsControls.Controls"
             xmlns:HowLongToBeat="clr-namespace:HowLongToBeat"
             xmlns:hltm="clr-namespace:HowLongToBeat.Models"
             xmlns:hlte="clr-namespace:HowLongToBeat.Models.Enumerations"
             xmlns:hlts="clr-namespace:HowLongToBeat.Services"
             xmlns:hltb="clr-namespace:HowLongToBeat.Behaviors"
             xmlns:converters="clr-namespace:CommonPluginsShared.Converters"
             xmlns:converters1="clr-namespace:CommonPlayniteShared.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance HowLongToBeat:HowLongToBeatSettingsViewModel}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

            <ScrollViewer Margin="0,10,0,0">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>

                        <Button Name="PART_BtAuthenticate"
                                Content="{DynamicResource LOCCommonAuthenticateLabel}"
                                Grid.Column="0"
                                VerticalAlignment="Top"
                                MinWidth="110"
                                Margin="0,1,0,0"/>
                        <Label Name="PART_LbAuthenticate"
                               VerticalAlignment="Center"
                               Grid.Column="2"/>
                        <Label Name="PART_LbUserLogin"
                               Content="{Binding Settings.UserLogin}"
                               VerticalAlignment="Center"
                               Grid.Column="3"/>
                    </Grid>

                    <CheckBox Margin="0,10,0,0"
                              IsChecked="{Binding Settings.MenuInExtensions}"
                              Content="{DynamicResource LOCCommonMenuInExtensions}"/>

                    <CheckBox Margin="0,10,0,0"
                              IsChecked="{Binding Settings.ShowHltbImg}"
                              Content="{DynamicResource LOCHowLongToBeatShowHltbImage}"/>

                    <!-- Export controls -->
                    <Grid Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0"
                               Content="{DynamicResource LOCHowLongToBeatExport}"
                               VerticalAlignment="Center"/>
                        <Button Grid.Column="2"
                                x:Name="PART_BtnExportCsvComma"
                                Content="{DynamicResource LOCHowLongToBeatExportCsvComma}"
                                MinHeight="26"
                                VerticalAlignment="Center"/>
                        <Button Grid.Column="4"
                                x:Name="PART_BtnExportCsvSemicolon"
                                Content="{DynamicResource LOCHowLongToBeatExportCsvSemicolon}"
                                MinHeight="26"
                                VerticalAlignment="Center"/>
                        <Button Grid.Column="6"
                                x:Name="PART_BtnExportJson"
                                Content="{DynamicResource LOCHowLongToBeatExportJson}"
                                MinHeight="26"
                                VerticalAlignment="Center"/>
                    </Grid>

                    <Grid Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="PART_ExportFolder"
                                 Grid.Column="0"
                                 MinHeight="26"
                                 VerticalAlignment="Center"
                                 ToolTip="{DynamicResource LOCHowLongToBeatExportFolderTooltip}"/>
                        <Button Grid.Column="2"
                                x:Name="PART_BtnBrowseExportFolder"
                                Content="{DynamicResource LOCBrowseLabel}"
                                MinHeight="26"
                                VerticalAlignment="Center"/>
                    </Grid>

                    <Grid Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>

                        <CheckBox Name="cbEnableTag"
                                  VerticalAlignment="Center"
                                  IsChecked="{Binding Settings.EnableTag}"
                                  Content="{DynamicResource LOCHowLongToBeatAddTagDescription}"/>

                        <StackPanel Grid.Column="2"
                                    Orientation="Horizontal">
                            <Button x:Name="PART_BtnAddTag"
                                    Content="{DynamicResource LOCAddTitle}"
                                    Margin="0,0,10,0"
                                    IsEnabled="{Binding Settings.EnableTag}"
                                    VerticalAlignment="Center"/>
                            <Button x:Name="PART_BtnRemoveTag"
                                    Content="{DynamicResource LOCRemoveTitle}"
                                    IsEnabled="{Binding Settings.EnableTag}"
                                    VerticalAlignment="Bottom"
                                    Margin="0,0,0,1"/>
                        </StackPanel>
                    </Grid>

                    <StackPanel Margin="0,10,0,0">
                        <Label Content="{DynamicResource LOCHltbSelectDataType}"/>

                        <StackPanel Orientation="Horizontal"
                                    Margin="50,5,0,0">
                            <RadioButton GroupName="DataType"
                                         Content="{DynamicResource LOCHltbSelectDataTypeClassic}"
                                         IsChecked="{Binding Settings.UseHtltbClassic}"/>
                            <RadioButton GroupName="DataType"
                                         Content="{DynamicResource LOCHltbSelectDataTypeAverage}"
                                         IsChecked="{Binding Settings.UseHtltbAverage}"
                                         Margin="20,0,0,0"/>
                            <RadioButton GroupName="DataType"
                                         Content="{DynamicResource LOCHltbSelectDataTypeMedian}"
                                         IsChecked="{Binding Settings.UseHtltbMedian}"
                                         Margin="20,0,0,0"/>
                            <RadioButton GroupName="DataType"
                                         Content="{DynamicResource LOCHltbSelectDataTypeRushed}"
                                         IsChecked="{Binding Settings.UseHtltbRushed}"
                                         Margin="20,0,0,0"/>
                            <RadioButton GroupName="DataType"
                                         Content="{DynamicResource LOCHltbSelectDataTypeLeisure}"
                                         IsChecked="{Binding Settings.UseHtltbLeisure}"
                                         Margin="20,0,0,0"/>
                        </StackPanel>
                    </StackPanel>


                    <StackPanel Margin="0,10,0,0"
                                Orientation="Horizontal">
                        <Label Content="{DynamicResource LOCHltbPreferredTimeToBeat}"
                               Margin="0,0,10,0"/>

                        <ComboBox SelectedValue="{Binding Settings.PreferredForTimeToBeat}"
                                  VerticalAlignment="Center"
                                  Grid.Column="1"
                                  Grid.Row="2">
                            <ComboBox.ItemsSource>
                                <x:Array Type="{x:Type sys:Enum}">
                                    <hlte:TimeType>MainStory</hlte:TimeType>
                                    <hlte:TimeType>MainStoryExtra</hlte:TimeType>
                                    <hlte:TimeType>Completionist</hlte:TimeType>
                                </x:Array>
                            </ComboBox.ItemsSource>
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Converter={converters1:ObjectToStringConverter}}"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>


                    <CheckBox Margin="0,10,0,0"
                              IsChecked="{Binding Settings.UsedStartDateFromGameActivity}"
                              Content="{DynamicResource LOCHltbUsedStartDateFromGameActivity}"/>

                    <Grid Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>

                        <CheckBox IsChecked="{Binding Settings.AutoSetCurrentPlayTime}"
                                  Name="PART_AutoSetCurrentPlayTime"
                                  Content="{DynamicResource LOCHowLongToBeatAutoSetCurrentPlayTime}"/>
                        <Label Grid.Column="2"
                               FontFamily="{DynamicResource FontIcoFont}"
                               Content="&#xef19;"
                               ToolTip="{DynamicResource LOCHowLongToBeatInfoSetPlaytime}"/>
                    </Grid>

                    <CheckBox Margin="50,5,0,0"
                              IsChecked="{Binding Settings.AutoSetCurrentPlayTimeWithoutConfirmation}"
                              IsEnabled="{Binding ElementName=PART_AutoSetCurrentPlayTime, Path=IsChecked}"
                              Content="{DynamicResource LOCHowLongToBeatWithoutConfirmation}"/>

                    <CheckBox Margin="50,5,0,0"
                              IsChecked="{Binding Settings.EnableSucessNotification}"
                              IsEnabled="{Binding ElementName=PART_AutoSetCurrentPlayTime, Path=IsChecked}"
                              Content="{DynamicResource LOCHowLongToBeatEnableSuccessNotification}"/>


                    <CheckBox Margin="0,10,0,0"
                              IsChecked="{Binding Settings.AutoSetGameStatus}"
                              Name="PART_AutoSetGameStatusFromHltb"
                              Content="{DynamicResource LOCHowLongToBeatAutoSetGameStatus}"/>

                    <CheckBox Margin="0,10,0,0"
                              IsChecked="{Binding Settings.AutoSetGameStatusToHltb}"
                              Name="PART_AutoSetGameStatusToHltb"
                              Content="{DynamicResource LOCHowLongToBeatAutoSetGameStatusToHltb}"/>

                    <StackPanel Margin="50,5,0,0">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="IsEnabled" Value="False"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ElementName=PART_AutoSetGameStatusFromHltb, Path=IsChecked}"
                                                 Value="True">
                                        <Setter Property="IsEnabled" Value="True"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding ElementName=PART_AutoSetGameStatusToHltb, Path=IsChecked}" Value="True">
                                        <Setter Property="IsEnabled" Value="True"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>

                        <ScrollViewer HorizontalScrollBarVisibility="Auto"
                                      VerticalScrollBarVisibility="Disabled"
                                      PanningMode="HorizontalOnly">
                            <StackPanel>
                            <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="220"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="6"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>

                        <Label Grid.Column="0"
                               Grid.Row="0"
                               Grid.ColumnSpan="3"
                               Content="{DynamicResource LOCHowLongToBeatHltbStatusColumnHeader}"
                               FontSize="11"
                               Opacity="0.8"/>
                        <Label Grid.Column="4"
                               Grid.Row="0"
                               Grid.ColumnSpan="3"
                               Content="{DynamicResource LOCHowLongToBeatPlayniteDataColumnHeader}"
                               FontSize="11"
                               Opacity="0.8"/>
                        <Label Grid.Column="8"
                               Grid.Row="0"
                               Content="{DynamicResource LOCHowLongToBeatClearOtherListsColumnHeader}"
                               FontSize="11"
                               Opacity="0.8"
                               ToolTip="{DynamicResource LOCHowLongToBeatClearOtherListsTooltip}"/>

                        <Label Grid.Column="0"
                               Grid.Row="2"
                               Content="{DynamicResource LOCHowLongToBeatPlaying}"/>
                        <ComboBox Grid.Column="2"
                                  Grid.Row="2"
                                  x:Name="PART_GameStatusPlaying"
                                  SelectedValuePath="Id"
                                  DisplayMemberPath="Name"
                                  SelectedValue="{Binding Settings.GameStatusPlaying}"/>
                        <CheckBox Grid.Column="4"
                                  Grid.Row="2"
                                  IsEnabled="{Binding ElementName=PART_AutoSetGameStatusToHltb, Path=IsChecked}"
                                  IsChecked="{Binding Settings.AutoSetToHltbPlayingSendPlaytime}"
                                  Content="{DynamicResource LOCHowLongToBeatSyncPlaytimeShort}"
                                  ToolTip="{DynamicResource LOCHowLongToBeatAutoSetGameStatusToHltbPlayingPlaytime}"/>
                        <CheckBox Grid.Column="8"
                                  Grid.Row="2"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Center"
                                  IsEnabled="{Binding ElementName=PART_AutoSetGameStatusToHltb, Path=IsChecked}"
                                  IsChecked="{Binding Settings.ToHltbPlayingListSync.ClearOtherLists}"
                                  ToolTip="{DynamicResource LOCHowLongToBeatClearOtherListsTooltip}"/>

                        <Label Grid.Column="0"
                               Grid.Row="4"
                               Content="{DynamicResource LOCHowLongToBeatCompleted}"/>
                        <ComboBox Grid.Column="2"
                                  Grid.Row="4"
                                  x:Name="PART_GameStatusCompleted"
                                  SelectedValuePath="Id"
                                  DisplayMemberPath="Name"
                                  SelectedValue="{Binding Settings.GameStatusCompleted}"/>
                        <CheckBox Grid.Column="4"
                                  Grid.Row="4"
                                  IsEnabled="{Binding ElementName=PART_AutoSetGameStatusToHltb, Path=IsChecked}"
                                  IsChecked="{Binding Settings.AutoSetToHltbCompletedSendPlaytime}"
                                  Content="{DynamicResource LOCHowLongToBeatSyncPlaytimeShort}"
                                  ToolTip="{DynamicResource LOCHowLongToBeatAutoSetGameStatusToHltbCompletedPlaytime}"/>
                        <CheckBox Grid.Column="6"
                                  Grid.Row="4"
                                  IsEnabled="{Binding ElementName=PART_AutoSetGameStatusToHltb, Path=IsChecked}"
                                  IsChecked="{Binding Settings.AutoSetToHltbCompletedSendCompletionDate}"
                                  Content="{DynamicResource LOCHowLongToBeatSyncDateShort}"
                                  ToolTip="{DynamicResource LOCHowLongToBeatAutoSetGameStatusToHltbCompletedDate}">
                            <CheckBox.Style>
                                <Style TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
                                    <Setter Property="IsEnabled"
                                            Value="False"/>
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding ElementName=PART_AutoSetGameStatusToHltb, Path=IsChecked}" Value="True"/>
                                                <Condition Binding="{Binding Settings.AutoSetToHltbCompletedSendPlaytime}" Value="True"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="IsEnabled" Value="True"/>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </CheckBox.Style>
                        </CheckBox>
                        <CheckBox Grid.Column="8"
                                  Grid.Row="4"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Center"
                                  IsEnabled="{Binding ElementName=PART_AutoSetGameStatusToHltb, Path=IsChecked}"
                                  IsChecked="{Binding Settings.ToHltbCompletedListSync.ClearOtherLists}"
                                  ToolTip="{DynamicResource LOCHowLongToBeatClearOtherListsTooltip}"/>

                        <Label Grid.Column="0"
                               Grid.Row="6"
                               Content="{DynamicResource LOCHowLongToBeatCompletionist}"/>
                        <ComboBox Grid.Column="2"
                                  Grid.Row="6"
                                  x:Name="PART_GameStatusCompletionist"
                                  SelectedValuePath="Id"
                                  DisplayMemberPath="Name"
                                  SelectedValue="{Binding Settings.GameStatusCompletionist}"/>
                        <CheckBox Grid.Column="4"
                                  Grid.Row="6"
                                  IsEnabled="{Binding ElementName=PART_AutoSetGameStatusToHltb, Path=IsChecked}"
                                  IsChecked="{Binding Settings.AutoSetToHltbCompletionistSendPlaytime}"
                                  Content="{DynamicResource LOCHowLongToBeatSyncPlaytimeShort}"
                                  ToolTip="{DynamicResource LOCHowLongToBeatAutoSetGameStatusToHltbCompletionistPlaytime}"/>
                        <CheckBox Grid.Column="6"
                                  Grid.Row="6"
                                  IsEnabled="{Binding ElementName=PART_AutoSetGameStatusToHltb, Path=IsChecked}"
                                  IsChecked="{Binding Settings.AutoSetToHltbCompletionistSendCompletionDate}"
                                  Content="{DynamicResource LOCHowLongToBeatSyncDateShort}"
                                  ToolTip="{DynamicResource LOCHowLongToBeatAutoSetGameStatusToHltbCompletionistDate}">
                            <CheckBox.Style>
                                <Style TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
                                    <Setter Property="IsEnabled"
                                            Value="False"/>
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding ElementName=PART_AutoSetGameStatusToHltb, Path=IsChecked}" Value="True"/>
                                                <Condition Binding="{Binding Settings.AutoSetToHltbCompletionistSendPlaytime}" Value="True"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="IsEnabled" Value="True"/>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </CheckBox.Style>
                        </CheckBox>
                        <CheckBox Grid.Column="8"
                                  Grid.Row="6"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Center"
                                  IsEnabled="{Binding ElementName=PART_AutoSetGameStatusToHltb, Path=IsChecked}"
                                  IsChecked="{Binding Settings.ToHltbCompletionistListSync.ClearOtherLists}"
                                  ToolTip="{DynamicResource LOCHowLongToBeatClearOtherListsTooltip}"/>

                        <Label Grid.Column="0"
                               Grid.Row="8"
                               Content="{DynamicResource LOCHltbUserListBacklog}"/>
                        <ComboBox Grid.Column="2"
                                  Grid.Row="8"
                                  x:Name="PART_GameStatusBacklog"
                                  SelectedValuePath="Id"
                                  DisplayMemberPath="Name"
                                  SelectedValue="{Binding Settings.GameStatusBacklog}"/>
                        <CheckBox Grid.Column="8"
                                  Grid.Row="8"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Center"
                                  IsEnabled="{Binding ElementName=PART_AutoSetGameStatusToHltb, Path=IsChecked}"
                                  IsChecked="{Binding Settings.ToHltbBacklogListSync.ClearOtherLists}"
                                  ToolTip="{DynamicResource LOCHowLongToBeatClearOtherListsTooltip}"/>

                        <Label Grid.Column="0"
                               Grid.Row="10"
                               Content="{DynamicResource LOCHltbUserListReplays}"/>
                        <ComboBox Grid.Column="2"
                                  Grid.Row="10"
                                  x:Name="PART_GameStatusReplays"
                                  SelectedValuePath="Id"
                                  DisplayMemberPath="Name"
                                  SelectedValue="{Binding Settings.GameStatusReplays}"/>
                        <CheckBox Grid.Column="8"
                                  Grid.Row="10"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Center"
                                  IsEnabled="{Binding ElementName=PART_AutoSetGameStatusToHltb, Path=IsChecked}"
                                  IsChecked="{Binding Settings.ToHltbReplaysListSync.ClearOtherLists}"
                                  ToolTip="{DynamicResource LOCHowLongToBeatClearOtherListsTooltip}"/>

                        <Label Grid.Column="0"
                               Grid.Row="12"
                               Content="{DynamicResource LOCHltbUserListRetired}"/>
                        <ComboBox Grid.Column="2"
                                  Grid.Row="12"
                                  x:Name="PART_GameStatusRetired"
                                  SelectedValuePath="Id"
                                  DisplayMemberPath="Name"
                                  SelectedValue="{Binding Settings.GameStatusRetired}"/>
                        <CheckBox Grid.Column="8"
                                  Grid.Row="12"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Center"
                                  IsEnabled="{Binding ElementName=PART_AutoSetGameStatusToHltb, Path=IsChecked}"
                                  IsChecked="{Binding Settings.ToHltbRetiredListSync.ClearOtherLists}"
                                  ToolTip="{DynamicResource LOCHowLongToBeatClearOtherListsTooltip}"/>
                            </Grid>

                        <Label Margin="0,12,0,4"
                               Content="{DynamicResource LOCHowLongToBeatAlwaysKeepListsTitle}"
                               ToolTip="{DynamicResource LOCHowLongToBeatAlwaysKeepListsTooltip}"/>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox Margin="0,0,12,0"
                                      Content="{DynamicResource LOCHowLongToBeatPlaying}"
                                      IsChecked="{Binding Settings.ToHltbAlwaysKeepLists.AlwaysKeepPlayingIfPresent}"/>
                            <CheckBox Margin="0,0,12,0"
                                      Content="{DynamicResource LOCHltbUserListBacklog}"
                                      IsChecked="{Binding Settings.ToHltbAlwaysKeepLists.AlwaysKeepBacklogIfPresent}"/>
                            <CheckBox Margin="0,0,12,0"
                                      Content="{DynamicResource LOCHltbUserListReplays}"
                                      IsChecked="{Binding Settings.ToHltbAlwaysKeepLists.AlwaysKeepReplayIfPresent}"/>
                            <CheckBox Margin="0,0,12,0"
                                      Content="{DynamicResource LOCHowLongToBeatCompleted}"
                                      IsChecked="{Binding Settings.ToHltbAlwaysKeepLists.AlwaysKeepCompletedIfPresent}"/>
                            <CheckBox Content="{DynamicResource LOCHltbUserListRetired}"
                                      IsChecked="{Binding Settings.ToHltbAlwaysKeepLists.AlwaysKeepRetiredIfPresent}"/>
                        </StackPanel>
                            </StackPanel>
                        </ScrollViewer>
                    </StackPanel>


                    <StackPanel Margin="0,30,0,0">
                        <Label Content="{DynamicResource LOCHowLongToBeatUserData}"
                               FontSize="18"/>
                        <Separator Margin="0,0,0,10"/>

                    </StackPanel>

                    <CheckBox Margin="0,10,0,0"
                              IsChecked="{Binding Settings.EnableProgressBarInDataView}"
                              Content="{DynamicResource LOCHltbEnableProgressBarInDataView}"/>

                    <Grid Margin="0,30,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="35"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0"
                                    Grid.Column="0"
                                    Grid.ColumnSpan="3">
                            <Label Content="{DynamicResource LOCHowLongToBeatAutoData}"
                                   FontSize="18"/>
                            <Separator Margin="0,0,0,10"/>
                        </StackPanel>

                        <CheckBox Name="cbAutoImport"
                                  Grid.Row="1"
                                  IsChecked="{Binding Settings.AutoImport}"
                                  Grid.Column="0"
                                  VerticalAlignment="Center"
                                  Grid.ColumnSpan="3"
                                  Content="{DynamicResource LOCCommonAutoImport}"/>

                        <CheckBox Name="cbAutoAccept"
                                  Grid.Row="2"
                                  IsChecked="{Binding Settings.AutoAccept}"
                                  Grid.Column="0"
                                  VerticalAlignment="Center"
                                  Grid.ColumnSpan="3"
                                  Content="{DynamicResource LOCHowLongToBeatAutoAccept}"/>

                        <CheckBox Name="cbShowWhenMismatch"
                                  Grid.Row="3"
                                  IsChecked="{Binding Settings.ShowWhenMismatch}"
                                  Grid.Column="0"
                                  Grid.ColumnSpan="3"
                                  VerticalAlignment="Center"
                                  Content="{DynamicResource LOCHowLongToBeatShowWhenMismatch}"/>

                        <CheckBox Grid.Row="4"
                                  IsChecked="{Binding Settings.UseMatchValue}"
                                  Grid.Column="0"
                                  VerticalAlignment="Center"
                                  Content="{DynamicResource LOCHowLongToBeatUseMatchValue}"/>

                        <StackPanel Orientation="Horizontal"
                                    Grid.Row="4"
                                    Grid.Column="2">
                            <Slider Width="200"
                                    VerticalAlignment="Center"
                                    Name="PART_SliderMAtchValue"
                                    Value="{Binding Settings.MatchValue, FallbackValue=95}"
                                    Minimum="75"
                                    Maximum="100"
                                    IsSnapToTickEnabled="True"
                                    TickFrequency="1"/>
                            <Label Content="{Binding ElementName=PART_SliderMAtchValue, Path=Value}"
                                   ContentStringFormat=" {0}%"
                                   Width="50"
                                   VerticalAlignment="Center"
                                   Margin="10,0,0,0"
                                   HorizontalAlignment="Center"/>
                        </StackPanel>

                        <Grid Grid.Column="0"
                              Grid.ColumnSpan="3"
                              Grid.Row="6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                       Text="{DynamicResource LOCCommonDatabase}"
                                       VerticalAlignment="Center"/>
                            <Button Grid.Column="2"
                                    Name="btAddData"
                                    Content="{DynamicResource LOCUpdateAll}"
                                    VerticalAlignment="Center"/>
                            <Button Grid.Column="4"
                                    Name="btRemoveData"
                                    Content="{DynamicResource LOCClearAll}"
                                    VerticalAlignment="Center "/>
                        </Grid>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
</UserControl>
