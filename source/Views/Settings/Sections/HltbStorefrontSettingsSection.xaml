<UserControl x:Class="HowLongToBeat.Views.HltbStorefrontSettingsSection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:HowLongToBeat="clr-namespace:HowLongToBeat"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance HowLongToBeat:HowLongToBeatSettingsViewModel}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <DockPanel LastChildFill="True">
        <TextBlock DockPanel.Dock="Top"
                   Style="{DynamicResource BaseTextBlockStyle}"
                   TextWrapping="Wrap"
                   Text="{DynamicResource LOCHowLongToBeatStorefrontMappingDescription}"/>

        <ListView ItemsSource="{Binding Settings.StorefrontElements}"
                  Margin="0,10,0,0"
                  MinHeight="100"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemContainerStyle>
                <Style TargetType="{x:Type ListViewItem}"
                       BasedOn="{StaticResource {x:Type ListViewItem}}">
                    <Setter Property="Focusable"
                            Value="False"/>
                    <Setter Property="HorizontalContentAlignment"
                            Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.View>
                <GridView>
                    <GridViewColumn Width="380">
                        <GridViewColumn.Header>
                            <TextBlock Style="{DynamicResource BaseTextBlockStyle}"
                                       FontWeight="SemiBold"
                                       Text="{DynamicResource LOCHowLongToBeatMappingColumnPlayniteOrigin}"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Margin="0,4">
                                    <TextBlock Style="{DynamicResource BaseTextBlockStyle}"
                                               FontWeight="SemiBold"
                                               Text="{Binding SourceName}">
                                        <TextBlock.ToolTip>
                                            <ToolTip>
                                                <StackPanel>
                                                    <TextBlock Style="{DynamicResource BaseTextBlockStyle}"
                                                               FontWeight="SemiBold"
                                                               Text="{DynamicResource LOCHowLongToBeatMappingPlayniteSourceIdToolTip}"/>
                                                    <TextBlock Style="{DynamicResource BaseTextBlockStyle}"
                                                               Text="{Binding PlayniteSourceId}"
                                                               TextWrapping="Wrap"
                                                               MaxWidth="360"/>
                                                </StackPanel>
                                            </ToolTip>
                                        </TextBlock.ToolTip>
                                    </TextBlock>

                                    <TextBlock Style="{DynamicResource BaseTextBlockStyle}"
                                               Text="{Binding PlayniteSourceId}"
                                               Opacity="0.75"
                                               FontSize="{DynamicResource FontSizeSmall}"
                                               Margin="0,2,0,0"
                                               TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Width="280">
                        <GridViewColumn.Header>
                            <TextBlock Style="{DynamicResource BaseTextBlockStyle}"
                                       FontWeight="SemiBold"
                                       Text="{DynamicResource LOCHowLongToBeatMappingColumnHltbTarget}"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Margin="0,4">
                                    <ComboBox ItemsSource="{Binding StoreFrontElements}"
                                              SelectedValuePath="HltbStorefrontId"
                                              DisplayMemberPath="HltbStorefrontName"
                                              SelectedValue="{Binding HltbStorefrontId}"
                                              ToolTip="{Binding HltbStorefrontName}"/>

                                    <TextBlock Margin="0,4,0,0"
                                               FontSize="{DynamicResource FontSizeSmall}"
                                               Opacity="0.75"
                                               FontStyle="Italic"
                                               Text="{DynamicResource LOCHowLongToBeatMappingStorefrontNotSetHint}">
                                        <TextBlock.Style>
                                            <Style TargetType="{x:Type TextBlock}"
                                                   BasedOn="{StaticResource BaseTextBlockStyle}">
                                                <Setter Property="Visibility"
                                                        Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsStorefrontConfigured}"
                                                                 Value="False">
                                                        <Setter Property="Visibility"
                                                                Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</UserControl>
