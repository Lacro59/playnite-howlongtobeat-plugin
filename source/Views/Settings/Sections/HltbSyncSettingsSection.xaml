<UserControl x:Class="HowLongToBeat.Views.HltbSyncSettingsSection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:AttachedProperties="clr-namespace:CommonPluginsShared.Controls"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:controls="clr-namespace:CommonPluginsControls.Controls"
             xmlns:HowLongToBeat="clr-namespace:HowLongToBeat"
             xmlns:hltm="clr-namespace:HowLongToBeat.Models"
             xmlns:hlte="clr-namespace:HowLongToBeat.Models.Enumerations"
             xmlns:hlts="clr-namespace:HowLongToBeat.Services"
             xmlns:hltb="clr-namespace:HowLongToBeat.Behaviors"
             xmlns:converters="clr-namespace:CommonPluginsShared.Converters"
             xmlns:converters1="clr-namespace:CommonPlayniteShared.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance HowLongToBeat:HowLongToBeatSettingsViewModel}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

            <DockPanel Margin="0,10,0,0">
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Style="{DynamicResource BaseTextBlockStyle}"
                               TextWrapping="Wrap"
                               Text="{DynamicResource LOCHowLongToBeatIgnoreSyncDescription}"/>

                    <Button Margin="0,10,0,0"
                            x:Name="PART_BtnIgnoreSyncAddGame"
                            Content="{DynamicResource LOCHowLongToBeatIgnoreSyncAddGame}"
                            MinHeight="26"
                            HorizontalAlignment="Left"/>
                </StackPanel>

                <ListBox x:Name="PART_IgnoreSyncList"
                         Margin="0,10,0,0">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem"
                               BasedOn="{StaticResource {x:Type ListBoxItem}}">
                            <Setter Property="Height"
                                    Value="32"/>
                            <Setter Property="Padding"
                                    Value="4,0"/>
                            <Setter Property="HorizontalContentAlignment"
                                    Value="Stretch"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Height="28">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="28"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0"
                                           Style="{DynamicResource ListBoxExtendStyleTextBlock}"
                                           Text="{Binding Name}"
                                           VerticalAlignment="Center"
                                           TextTrimming="CharacterEllipsis"/>

                                <Button x:Name="PART_RemoveButton"
                                        Grid.Column="1"
                                        Width="28"
                                        Height="28"
                                        Padding="0"
                                        VerticalAlignment="Center"
                                        Opacity="0"
                                        IsHitTestVisible="False"
                                        Tag="{Binding}"
                                        ToolTip="{DynamicResource LOCHowLongToBeatIgnoreSyncRemoveTooltip}"
                                        ContentTemplate="{DynamicResource ClearTextIconTemplate}"/>
                            </Grid>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsMouseOver}"
                                             Value="True">
                                    <Setter TargetName="PART_RemoveButton"
                                            Property="Opacity"
                                            Value="1"/>
                                    <Setter TargetName="PART_RemoveButton"
                                            Property="IsHitTestVisible"
                                            Value="True"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
</UserControl>
